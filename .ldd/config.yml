ledger:
  mode: local
  draft_directory: docs/tickets/_drafts
  archive_directory: docs/tickets/_archive
  local_id_prefix: LDD

tracker:
  provider: local
  repo: null
  project: null
  default_branch: main

artifacts:
  root: docs/tickets

branches:
  prd: ldd/prd/{ticket}
  sdd_plan: ldd/sdd-plan/{ticket}
  implementation: ldd/impl/{ticket}

prs:
  prd_title: "PRD: {title}"
  sdd_plan_title: "SDD + Plan: {title}"
  implementation_title: "{title}"

renderer:
  plan_html: true

adr:
  directory: docs/adr
  update_policy: immutable_superseded
  filename_style: dated_slug
