description = "Gather PM-grade research inputs before LDD scoping."
prompt = """
Run `/ldd:research` using the `ldd-research` Agent Skill.

If the `ldd-research` skill is available, activate it and follow its `SKILL.md` exactly. Treat the skill as canonical; this TOML file is only a command router.

User arguments:
{{args}}
"""
