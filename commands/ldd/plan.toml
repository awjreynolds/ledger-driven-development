description = "Write the LDD implementation plan and plan HTML."
prompt = """
Run `/ldd:plan` using the `ldd-plan` Agent Skill.

If the `ldd-plan` skill is available, activate it and follow its `SKILL.md` exactly. Treat the skill as canonical; this TOML file is only a command router.

User arguments:
{{args}}
"""
