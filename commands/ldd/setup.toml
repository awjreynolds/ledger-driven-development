description = "Bootstrap LDD in the current GitHub repository."
prompt = """
Run `/ldd:setup` using the `ldd-setup` Agent Skill.

If the `ldd-setup` skill is available, activate it and follow its `SKILL.md` exactly. Treat the skill as canonical; this TOML file is only a command router.

User arguments:
{{args}}
"""
