description = "Execute an approved LDD implementation plan."
prompt = """
Run `/ldd:implement` using the `ldd-implement` Agent Skill.

If the `ldd-implement` skill is available, activate it and follow its `SKILL.md` exactly. Treat the skill as canonical; this TOML file is only a command router.

User arguments:
{{args}}
"""
