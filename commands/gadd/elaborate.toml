description = "Fill product detail inside existing GADD PRD scope."
prompt = """
Run `/gadd:elaborate` using the `gadd-elaborate` Agent Skill.

If the `gadd-elaborate` skill is available, activate it and follow its `SKILL.md` exactly. Treat the skill as canonical; this TOML file is only a command router.

User arguments:
{{args}}
"""
