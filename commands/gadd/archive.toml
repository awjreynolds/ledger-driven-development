description = "Archive closed GADD ticket files after workflow closure."
prompt = """
Run `/gadd:archive` using the `gadd-archive` Agent Skill.

If the `gadd-archive` skill is available, activate it and follow its `SKILL.md` exactly. Treat the skill as canonical; this TOML file is only a command router.

User arguments:
{{args}}
"""
