description = "Write the GADD implementation plan and plan HTML."
prompt = """
Run `/gadd:plan` using the `gadd-plan` Agent Skill.

If the `gadd-plan` skill is available, activate it and follow its `SKILL.md` exactly. Treat the skill as canonical; this TOML file is only a command router.

User arguments:
{{args}}
"""
