interface:
  display_name: "GADD Scope"
  short_description: "Write PRD scope boundaries"
  default_prompt: "Use $gadd-scope to run /gadd:scope for issue #1."

policy:
  allow_implicit_invocation: true
