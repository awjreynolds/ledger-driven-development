interface:
  display_name: "GAPS Generate"
  short_description: "Generate GAPS skill package skeletons"
  default_prompt: "Use $gaps-generate to generate a skill package preview from a GAPS ga-process.yml file."

policy:
  allow_implicit_invocation: true
